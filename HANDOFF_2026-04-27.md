# Handoff — Sessão 2026-04-27

Documento de retomada. Migração total do provider Nuvem Fiscal para Focus NFe.

---

## TL;DR — O que aconteceu hoje

**Migração completa de Nuvem Fiscal → Focus NFe.** Provider antigo apagado, provider novo criado, todas as rotas/serviços/schemas/configs adaptados. Smoke test end-to-end com mock passou (1ª chamada baixa documento, 2ª duplica via unique constraint).

---

## Decisões fechadas com o usuário

1. **Substituição total**: sem dual-provider, sem feature flag. Focus NFe vira o único caminho.
2. **Já há conta Focus** com algumas empresas cadastradas + tokens em mãos.
3. **Escopo do robô**:
   - NFe-recebidas via DF-e/manifestação (entradas, fluxo principal)
   - Notas emitidas via Focus (quando esse caminho for adotado)
   - NFSe via Focus (cobertura limitada por município)
4. **Limitação aceita**: notas emitidas pelos clientes em **outros sistemas/ERPs** (SAP, Bling, etc.) não terão mais caminho automático de download. Focus NFe não tem esse recurso. Caso saiu do produto.

---

## Arquivos tocados

### Criados
- `backend/app/providers/focus_nfe.py` — `FocusNFeProvider` stateless (token por método). Inclui mocks (`USE_MOCK_FOCUS_NFE`), exceptions (`FocusNFeError`, `FocusEmpresaNaoCadastradaError`, `FocusTokenAusenteError`, `FocusManifestacaoError`) e dataclass `DocumentoRecebidoFocus`.
- `backend/app/providers/_common.py` — `parse_data_emissao` movida para módulo compartilhado.
- `backend/alembic/versions/20260427_0003_focus_nfe.py` — adiciona `empresas.focus_token` (String 128, nullable). Testada upgrade/downgrade.

### Modificados
- `backend/app/config.py` — removidas `use_mock_nuvem_fiscal`, `nuvem_*` (7 vars). Adicionadas `use_mock_focus_nfe`, `focus_base_url`, `focus_ambiente`, `focus_master_token`.
- `backend/.env` + `.env.example` — espelham as mudanças.
- `backend/app/main.py` — `mock_provider` no `/health` agora usa `use_mock_focus_nfe`.
- `backend/app/models/empresa.py` — adicionada coluna `focus_token: str | None`. `__repr__` customizado para nunca expor o token.
- `backend/app/services/robo_xml.py` — refatorado: `baixar_distribuicao_empresa` usa Focus, NSU incremental por chave de 44 dígitos. Métodos `baixar_nfe_empresa/cte_empresa/nfse_empresa` removidos (fora de escopo). `baixar_distribuicao_empresa_completa` e `baixar_distribuicao_multiempresas` são os pontos de entrada do robô diário.
- `backend/app/services/empresa_integracao.py` — drasticamente simplificado. Métodos `sync_empresa(payload, cert_bytes, cert_filename, cert_password)`, `importar_token(token)`, `status_integracao()` que retorna `{empresa_local_id, empresa_local_cnpj, tem_token, empresa_focus}` sem expor tokens.
- `backend/app/schemas/integracao_schema.py` — purgado: `EmpresaConfigCte/CteOs/Nfe/Nfse/DistNfePayload`, schemas auxiliares (`RegTrib`, `Rps`, `Prefeitura`, `AmbienteConfig`, `EmpresaConfigFiscalBase`, `CertificadoBase64Payload`). Renomeados `EnderecoNuvemFiscalSchema → EnderecoFocusSchema`, `EmpresaNuvemFiscalPayload → EmpresaFocusPayload`. Adicionado `EmpresaFocusTokenPayload`. `StatusIntegracaoEmpresaRead` simplificado.
- `backend/app/routes/empresas.py` — removidas 13 rotas obsoletas (`/integracao/sincronizar`, `/integracao/nuvem`, `/certificado` get/put-base64/delete, todas as 10 `/configuracoes/{nfe,cte,cteos,nfse,distnfe}`). Adicionadas: `PUT /focus` (multipart com payload_json + cert + senha), `PUT /focus/certificado` (renovação), `PUT /focus/token` (import manual), `GET /focus/status`.
- `backend/app/routes/robo.py` — removidas `POST /robo/nfe`, `/robo/cte`, `/robo/nfse`. `/robo/empresa` e `/robo/multiempresas` viram atalhos para distribuição. `/robo/distribuicao` agora consulta Focus.
- `backend/app/workers/tasks.py` — `executar_download_diario` chama `baixar_distribuicao_multiempresas`.
- `README.md` — reescrito para Focus NFe. Documenta limitações (notas emitidas em outros ERPs ficam fora; cobertura NFSe por município; janela 90 dias).

### Deletados
- `backend/app/providers/nuvem_fiscal.py` — apagado por inteiro (568 linhas removidas).

---

## Critérios de aceitação atendidos

✅ Provider Focus NFe criado com 14 métodos públicos
✅ Mocks funcionam para todos os métodos relevantes do robô
✅ Migration `20260427_0003` aplica e reverte (testada)
✅ Backend importa sem erros (`from app.main import app` → 27 rotas)
✅ Health `/health` retorna `mock_provider: true`
✅ `PUT /api/v1/empresas/1/focus/token` aceita token e persiste em `empresas.focus_token`
✅ `GET /api/v1/empresas/1/focus/status` retorna `tem_token: true` sem expor token nem nas chaves Focus
✅ `POST /api/v1/robo/distribuicao` baixa documento mockado (`processados: 1, baixados: 1`)
✅ Repetição de `/robo/distribuicao` retorna `duplicados: 1` (idempotência via unique constraint)
✅ XML é gravado em `storage/xmls/{cnpj}/nfe/{ano}/{mes}/{chave}.xml`
✅ `documentos_fiscais.origem` = `recebida` para itens vindos da distribuição
✅ Nenhum import remanescente de `nuvem_fiscal`, `NuvemFiscal*` ou `NUVEM_*` em todo o repo

---

## Bugs encontrados e corrigidos durante o smoke test

1. **`RoboResultado.__dict__` falhou** porque o dataclass usa `slots=True`. Corrigido para `dataclasses.asdict()` em `routes/robo.py` e `services/robo_xml.py`.
2. **Mock gerava chave com letras** ("NFE" no meio), e o filtro de processamento exige `.isdigit()`. Mock corrigido para produzir chave 100% numérica de 44 dígitos.

---

## Como subir o ambiente local amanhã

### 1. Banco
SQLite em `C:\Users\pacpa\AppData\Local\Temp\pac_xml_demo.db` está com `20260427_0003` aplicada.

Se o tmp for limpo, recriar:
```powershell
cd "pac-xml-downloader\backend"
python -m alembic upgrade head
```

### 2. Backend
```powershell
cd "pac-xml-downloader\backend"
python -m uvicorn app.main:app --host 127.0.0.1 --port 8000 --log-level warning
```
Swagger: http://127.0.0.1:8000/docs

### 3. Frontend
```powershell
cd "pac-xml-downloader\frontend"
npm run dev
```
URL: http://localhost:3000 → redireciona para `/login`.

### 4. Login
- E-mail: `admin@pacxml.com.br`
- Senha: `admin123`

### 5. Testar robô (mock)
```bash
TOKEN=$(curl -s -X POST http://127.0.0.1:8000/api/v1/auth/login -H "Content-Type: application/json" -d '{"email":"admin@pacxml.com.br","password":"admin123"}' | python -c "import json,sys;print(json.loads(sys.stdin.read())['access_token'])")

# Importar token mock para empresa 1
curl -X PUT http://127.0.0.1:8000/api/v1/empresas/1/focus/token \
  -H "Authorization: Bearer $TOKEN" -H "Content-Type: application/json" \
  -d '{"token":"mock-empresa-1-token-xyz"}'

# Rodar robô
curl -X POST http://127.0.0.1:8000/api/v1/robo/distribuicao \
  -H "Authorization: Bearer $TOKEN" -H "Content-Type: application/json" \
  -d '{"empresa_id":1,"data_inicio":"2026-01-01T00:00:00","data_fim":"2026-12-31T23:59:59"}'
```

---

## Para sair do mock e ir para API real

1. No painel da Focus NFe (https://app.focusnfe.com.br/), cadastre cada empresa cliente com seu certificado A1 + senha.
2. Copie o token gerado para a empresa (produção ou homologação).
3. No nosso sistema: `PUT /api/v1/empresas/{id}/focus/token` com o token real.
4. Edite `.env`: `USE_MOCK_FOCUS_NFE=false`.
5. Reinicie o backend.
6. Rode `POST /api/v1/robo/distribuicao` para uma janela de 7 dias retroativos.
7. Inspecione XMLs em `storage/xmls/{cnpj}/nfe/...`.

**Atenção**: a primeira execução pega tudo (até 90 dias) porque NSU começa em zero. Execuções subsequentes pegam só o delta via NSU incremental.

---

## Próximos passos pendentes

### Curto prazo (1 sessão cada)
- [ ] **Tela de cadastro/edição de empresa** no frontend (hoje só via Swagger/API).
- [ ] **Tela de upload de certificado + import de token** (rotas existem no backend).
- [ ] **Botão "executar robô"** por empresa.
- [ ] **Conectar dashboard, /documentos e /relatorios ao backend real** (hoje só /empresas é real).
- [ ] **Manifestação automática do destinatário** (estende janela 90 dias). Stub atual em `provider.manifestar_nfe_recebida` levanta `NotImplementedError`.
- [ ] **CT-e recebidos no robô**: provider já tem `listar_ctes_recebidos`; falta integrar em `RoboXMLService`.

### Médio prazo
- [ ] **Sair do mock com 1 empresa real** ponta a ponta.
- [ ] **Testes automatizados** (pytest) para `FocusNFeProvider` e `RoboXMLService`.
- [ ] **Trocar SQLite por Postgres** local antes de subir VPS.
- [ ] **Distribuição no Celery beat** para todas empresas (hoje só `/multiempresas` roda às 7h).

### Longo prazo
- [ ] **Criptografia em repouso do `focus_token`** (Fernet com chave derivada de `SECRET_KEY`) + migration one-shot.
- [ ] **Sentry + logs estruturados**.
- [ ] **Refresh token** para evitar logout a cada 8h.
- [ ] **Paginação/filtros/busca** nas listagens grandes.

---

## Onde estão as coisas

| Item | Caminho |
|---|---|
| Plano da migração | `C:\Users\pacpa\.claude\plans\vamos-ter-que-mudar-purring-tome.md` |
| Provider Focus | `pac-xml-downloader\backend\app\providers\focus_nfe.py` |
| Migration | `pac-xml-downloader\backend\alembic\versions\20260427_0003_focus_nfe.py` |
| Robô | `pac-xml-downloader\backend\app\services\robo_xml.py` |
| Rotas Focus | `pac-xml-downloader\backend\app\routes\empresas.py` (PUT /focus, /focus/token, GET /focus/status) |
| Banco SQLite (demo) | `C:\Users\pacpa\AppData\Local\Temp\pac_xml_demo.db` |
| `.env` ativo | `pac-xml-downloader\backend\.env` |
| Handoff anterior | `pac-xml-downloader\HANDOFF_2026-04-26.md` |
| Este handoff | `pac-xml-downloader\HANDOFF_2026-04-27.md` |
